# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Training artifacts (bulky, regenerable — keep only the final checkpoint/demo clips you want to showcase)
tensorboard_logs/
checkpoints/*
!checkpoints/.gitkeep

# Secrets — never commit a real API key
.env

# OS
.DS_Store
